# Copy to ".env" to enable the live TRIO Assistant (Claude API).
# Without this, the assistant runs in deterministic "Demo" mode (no key needed).

ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional overrides:
# CLAUDE_MODEL=claude-sonnet-4-6
# PORT=8787
